<Page x:Class="EquipmentManagement.Client.Pages.Materials.Materials"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EquipmentManagement.Client.Pages.Materials"
      d:DesignHeight="700" d:DesignWidth="1200"
      Title="Расходные материалы">

    <Page.Resources>
        <!-- Цветовая палитра по ТЗ -->
        <SolidColorBrush x:Key="PrimaryBlue" Color="#0060AC"/>
        <SolidColorBrush x:Key="AccentRed" Color="#E41613"/>
        <SolidColorBrush x:Key="NeutralGray" Color="#9EA1A2"/>
        <SolidColorBrush x:Key="LightGray" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="White" Color="White"/>
        <SolidColorBrush x:Key="DarkGray" Color="#333333"/>
        <SolidColorBrush x:Key="HoverBlue" Color="#E3F2FD"/>
        <SolidColorBrush x:Key="CardBackground" Color="#FAFAFA"/>
        <SolidColorBrush x:Key="ConsumableTypeColor1" Color="#2196F3"/>
        <SolidColorBrush x:Key="ConsumableTypeColor2" Color="#4CAF50"/>
        <SolidColorBrush x:Key="ConsumableTypeColor3" Color="#FF9800"/>

        <!-- Стиль основной кнопки -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBlue}"/>
            <Setter Property="Foreground" Value="{StaticResource White}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0077CC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#005A99"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль вторичной кнопки -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="{StaticResource White}"/>
            <Setter Property="BorderBrush" Value="{StaticResource NeutralGray}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource DarkGray}"/>
        </Style>

        <!-- Стиль кнопки "Назад" -->
        <Style x:Key="BackButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
            <Setter Property="FontWeight" Value="Medium"/>
        </Style>

        <!-- Стиль для поля поиска -->
        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource White}"/>
            <Setter Property="BorderBrush" Value="{StaticResource NeutralGray}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource DarkGray}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost" 
                                              VerticalAlignment="Center"
                                              HorizontalScrollBarVisibility="Hidden"/>
                                <TextBlock Text="Поиск по названию расходного материала..." 
                                           Foreground="{StaticResource NeutralGray}"
                                           Margin="12,0"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False"
                                           Visibility="Collapsed">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource AncestorType=TextBox}}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для карточек расходных материалов -->
        <Style x:Key="ConsumableCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource NeutralGray}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <!-- Стиль для тегов типа расходного материала -->
        <Style x:Key="ConsumableTypeTagStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>

        <!-- Стиль для тегов количества -->
        <Style x:Key="QuantityTagStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource HoverBlue}"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="Margin" Value="0,0,6,6"/>
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource LightGray}">
        <!-- Верхняя панель -->
        <Border Background="{StaticResource White}" 
                Height="70" 
                VerticalAlignment="Top"
                BorderBrush="{StaticResource NeutralGray}"
                BorderThickness="0,0,0,1">

            <Grid Margin="20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Заголовок -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📦" 
                               FontSize="24" 
                               VerticalAlignment="Center" 
                               Margin="0,0,10,0"/>
                    <TextBlock Text="РАСХОДНЫЕ МАТЕРИАЛЫ" 
                               FontSize="20" 
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBlue}"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="(1.11)" 
                               FontSize="12" 
                               Foreground="{StaticResource NeutralGray}"
                               VerticalAlignment="Bottom"
                               Margin="5,0,0,2"/>
                </StackPanel>

                <!-- Панель поиска и сортировки -->
                <StackPanel Grid.Column="1" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">

                    <!-- Поле поиска -->
                    <TextBox x:Name="search" 
                             Style="{StaticResource SearchTextBoxStyle}"
                             Width="300"
                             Height="38"
                             KeyDown="KeyDown_Search"/>

                    <!-- Кнопки сортировки -->
                    <StackPanel Orientation="Horizontal" Margin="10,0,0,0">
                        <Button x:Name="btnSortUp"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Content="▲"
                                Width="40"
                                Height="38"
                                FontSize="16"
                                Click="SortUp"
                                ToolTip="Сортировка по названию (А-Я)"/>

                        <Button x:Name="btnSortDown"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Content="▼"
                                Width="40"
                                Height="38"
                                FontSize="16"
                                Margin="5,0,0,0"
                                Click="SortDown"
                                ToolTip="Сортировка по названию (Я-А)"/>
                    </StackPanel>
                </StackPanel>

                <!-- Кнопка "Назад" -->
                <Button Grid.Column="2"
                        Style="{StaticResource BackButtonStyle}"
                        Click="Back"
                        Content="← Назад"
                        Width="100"
                        Height="38"/>
            </Grid>
        </Border>

        <!-- Основной контент -->
        <ScrollViewer Margin="20,80,20,70">
            <StackPanel x:Name="parent">

                <!-- Пример расходного материала -->
                <Border Style="{StaticResource ConsumableCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Информация о расходном материале -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Картридж для принтера HP 305A" 
                                       FontSize="16" 
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource DarkGray}"
                                       Margin="0,0,0,10"/>

                            <!-- Тип и количество -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <Border Style="{StaticResource ConsumableTypeTagStyle}"
                                        Background="#E3F2FD">
                                    <TextBlock Text="КАРТРИДЖ" 
                                               FontSize="12"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource ConsumableTypeColor1}"/>
                                </Border>

                                <Border Style="{StaticResource QuantityTagStyle}">
                                    <TextBlock Text="15 шт." 
                                               FontSize="12"/>
                                </Border>
                            </StackPanel>

                            <!-- Ответственный и дата -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="Ответственный:" 
                                           Foreground="{StaticResource NeutralGray}"
                                           Margin="0,0,5,0"/>
                                <TextBlock Text="Иванов И.И." 
                                           FontSize="13" 
                                           Foreground="{StaticResource PrimaryBlue}"/>

                                <TextBlock Text=" • " 
                                           Foreground="{StaticResource NeutralGray}"
                                           Margin="10,0,10,0"/>

                                <TextBlock Text="Поступило:" 
                                           Foreground="{StaticResource NeutralGray}"
                                           Margin="0,0,5,0"/>
                                <TextBlock Text="15.09.2023" 
                                           FontSize="13" 
                                           Foreground="{StaticResource DarkGray}"/>
                            </StackPanel>

                            <!-- Описание -->
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="Описание:" 
                                           Foreground="{StaticResource NeutralGray}"
                                           Margin="0,0,0,5"/>
                                <TextBlock Text="Оригинальный картридж для принтеров HP LaserJet Pro MFP M428fdw" 
                                           FontSize="13" 
                                           Foreground="{StaticResource DarkGray}"
                                           TextWrapping="Wrap"/>
                            </StackPanel>

                            <!-- Характеристики -->
                            <StackPanel>
                                <TextBlock Text="Характеристики:" 
                                           Foreground="{StaticResource NeutralGray}"
                                           Margin="0,0,0,5"/>

                                <WrapPanel>
                                    <Border Style="{StaticResource QuantityTagStyle}">
                                        <TextBlock Text="Цвет: черный" 
                                                   FontSize="12"/>
                                    </Border>
                                    <Border Style="{StaticResource QuantityTagStyle}">
                                        <TextBlock Text="Объем: 2500 стр." 
                                                   FontSize="12"/>
                                    </Border>
                                    <Border Style="{StaticResource QuantityTagStyle}">
                                        <TextBlock Text="Совместимость: HP 305A" 
                                                   FontSize="12"/>
                                    </Border>
                                </WrapPanel>
                            </StackPanel>
                        </StackPanel>

                        <!-- Кнопки действий -->
                        <StackPanel Grid.Column="2" 
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Right">
                            <Button Style="{StaticResource PrimaryButtonStyle}"
                                    Content="Передать"
                                    Width="120"
                                    Margin="0,0,0,8"/>

                            <Button Style="{StaticResource PrimaryButtonStyle}"
                                    Content="Редактировать"
                                    Width="120"
                                    Margin="0,0,0,8"/>

                            <Button Style="{StaticResource SecondaryButtonStyle}"
                                    Content="Удалить"
                                    Width="120"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Еще пример -->
                <Border Style="{StaticResource ConsumableCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Компьютерная мышь Logitech M185" 
                                       FontSize="16" 
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource DarkGray}"
                                       Margin="0,0,0,10"/>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <Border Style="{StaticResource ConsumableTypeTagStyle}"
                                        Background="#FFF3E0">
                                    <TextBlock Text="ПЕРИФЕРИЯ" 
                                               FontSize="12"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource ConsumableTypeColor3}"/>
                                </Border>

                                <Border Style="{StaticResource QuantityTagStyle}">
                                    <TextBlock Text="8 шт." 
                                               FontSize="12"/>
                                </Border>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="Ответственный:" 
                                           Foreground="{StaticResource NeutralGray}"
                                           Margin="0,0,5,0"/>
                                <TextBlock Text="Петров П.П." 
                                           FontSize="13" 
                                           Foreground="{StaticResource PrimaryBlue}"/>

                                <TextBlock Text=" • " 
                                           Foreground="{StaticResource NeutralGray}"
                                           Margin="10,0,10,0"/>

                                <TextBlock Text="Поступило:" 
                                           Foreground="{StaticResource NeutralGray}"
                                           Margin="0,0,5,0"/>
                                <TextBlock Text="20.08.2023" 
                                           FontSize="13" 
                                           Foreground="{StaticResource DarkGray}"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="Описание:" 
                                           Foreground="{StaticResource NeutralGray}"
                                           Margin="0,0,0,5"/>
                                <TextBlock Text="Беспроводная компьютерная мышь с USB-приемником, оптический сенсор" 
                                           FontSize="13" 
                                           Foreground="{StaticResource DarkGray}"
                                           TextWrapping="Wrap"/>
                            </StackPanel>

                            <StackPanel>
                                <TextBlock Text="Характеристики:" 
                                           Foreground="{StaticResource NeutralGray}"
                                           Margin="0,0,0,5"/>

                                <WrapPanel>
                                    <Border Style="{StaticResource QuantityTagStyle}">
                                        <TextBlock Text="Тип: беспроводная" 
                                                   FontSize="12"/>
                                    </Border>
                                    <Border Style="{StaticResource QuantityTagStyle}">
                                        <TextBlock Text="Цвет: черный" 
                                                   FontSize="12"/>
                                    </Border>
                                    <Border Style="{StaticResource QuantityTagStyle}">
                                        <TextBlock Text="Сенсор: оптический" 
                                                   FontSize="12"/>
                                    </Border>
                                </WrapPanel>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Grid.Column="2" 
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Right">
                            <Button Style="{StaticResource PrimaryButtonStyle}"
                                    Content="Передать"
                                    Width="120"
                                    Margin="0,0,0,8"/>

                            <Button Style="{StaticResource PrimaryButtonStyle}"
                                    Content="Редактировать"
                                    Width="120"
                                    Margin="0,0,0,8"/>

                            <Button Style="{StaticResource SecondaryButtonStyle}"
                                    Content="Удалить"
                                    Width="120"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Нижняя панель с информацией -->
        <Border Background="{StaticResource White}" 
                Height="60" 
                VerticalAlignment="Bottom"
                BorderBrush="{StaticResource NeutralGray}"
                BorderThickness="0,1,0,0">

            <Grid Margin="20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Информация о количестве -->
                <StackPanel Grid.Column="0" 
                            Orientation="Horizontal" 
                            VerticalAlignment="Center">
                    <TextBlock Text="Всего материалов:" 
                               Foreground="{StaticResource NeutralGray}"
                               Margin="0,0,5,0"/>
                    <TextBlock Text="2" 
                               FontWeight="Medium"
                               Foreground="{StaticResource PrimaryBlue}"/>

                    <TextBlock Text="•" 
                               Foreground="{StaticResource NeutralGray}"
                               Margin="10,0,5,0"/>

                    <TextBlock Text="Общее количество:" 
                               Foreground="{StaticResource NeutralGray}"
                               Margin="0,0,5,0"/>
                    <TextBlock Text="23 ед." 
                               FontWeight="Medium"
                               Foreground="{StaticResource ConsumableTypeColor2}"/>
                </StackPanel>

                <!-- Кнопки -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="exportDoc"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="ExportRasxodMaterials"
                            Content="Генерация документа"
                            Width="200"
                            Height="38"
                            Margin="0,0,10,0"/>

                    <Button x:Name="addBtn"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="Add"
                            Content="+ Добавить материал"
                            Width="200"
                            Height="38"
                            Visibility="Hidden"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Сообщение при пустом списке -->
        <Border x:Name="emptyMessage"
                Visibility="Collapsed"
                Background="{StaticResource White}"
                CornerRadius="8"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Width="500"
                Height="280">

            <StackPanel HorizontalAlignment="Center" 
                        VerticalAlignment="Center"
                        Margin="20">

                <TextBlock Text="📦" 
                           FontSize="40" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,15"/>

                <TextBlock Text="Расходные материалы" 
                           FontSize="18" 
                           FontWeight="SemiBold"
                           Foreground="{StaticResource DarkGray}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,8"/>

                <TextBlock Text="Добавляйте расходные материалы для оборудования учебного заведения." 
                           FontSize="13" 
                           Foreground="{StaticResource NeutralGray}"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           Margin="0,0,0,8"/>

                <TextBlock Text="По ТЗ: расходные материалы прикрепляются к оборудованию, ведется история ответственных, есть валидация полей" 
                           FontSize="12" 
                           Foreground="{StaticResource NeutralGray}"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           FontStyle="Italic"
                           TextWrapping="Wrap"
                           Margin="0,0,0,12"/>

                <Button Style="{StaticResource PrimaryButtonStyle}"
                        Content="Добавить первый материал"
                        Width="220"
                        Height="38"
                        Margin="0,10,0,0"
                        Click="Add"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
